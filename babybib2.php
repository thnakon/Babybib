<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Alan+Sans:wght@300..900&family=Comfortaa:wght@300..700&family=Prompt:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">    <style>
        
.comfortaa-1 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  /* Define the gradient as a background image */
  background-image: linear-gradient(to right, #180d36ff, #8b5cf6, #cf23cf, #180d36ff);
  
  /* Clip the background to the text shape */
  -webkit-background-clip: text; /* For WebKit browsers (Chrome, Safari, Edge) */
  background-clip: text;

  /* Make the text transparent so the gradient background shows through */
  -webkit-text-fill-color: transparent; /* For WebKit browsers */
  color: transparent; /* Fallback for other browsers (though less effective without background-clip support) */

  /* Optional: Add a fallback solid color for browsers that don't support the technique */
  /* background-color: #f32170; */ /* Choose a color that blends with your gradient */
}

.comfortaa-2 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  /* Define the gradient as a background image */
  background-image: linear-gradient(to right, #180d36ff, #92f65cff, #c3e12cff, #180d36ff);

  /* Clip the background to the text shape */
  -webkit-background-clip: text; /* For WebKit browsers (Chrome, Safari, Edge) */
  background-clip: text;

  /* Make the text transparent so the gradient background shows through */
  -webkit-text-fill-color: transparent; /* For WebKit browsers */
  color: transparent; /* Fallback for other browsers (though less effective without background-clip support) */

  /* Optional: Add a fallback solid color for browsers that don't support the technique */
  /* background-color: #f32170; */ /* Choose a color that blends with your gradient */
}
 .comfortaa-3 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  
}
.comfortaa-4 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  /* Define the gradient as a background image */
  background-image: linear-gradient(to right, #000000, #f6061eff, #f7ff1aff, #000000);
  
  /* Clip the background to the text shape */
  -webkit-background-clip: text; /* For WebKit browsers (Chrome, Safari, Edge) */
  background-clip: text;

  /* Make the text transparent so the gradient background shows through */
  -webkit-text-fill-color: transparent; /* For WebKit browsers */
  color: transparent; /* Fallback for other browsers (though less effective without background-clip support) */

  /* Optional: Add a fallback solid color for browsers that don't support the technique */
  background-color: #8b5cf6;  /* Choose a color that blends with your gradient */
}

.comfortaa-5 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  /* Define the gradient as a background image */
  background-image: linear-gradient(to right, #000000, #3606f6ff, #72b5f8ff, #000000);
  
  /* Clip the background to the text shape */
  -webkit-background-clip: text; /* For WebKit browsers (Chrome, Safari, Edge) */
  background-clip: text;

  /* Make the text transparent so the gradient background shows through */
  -webkit-text-fill-color: transparent; /* For WebKit browsers */
  color: transparent; /* Fallback for other browsers (though less effective without background-clip support) */

  /* Optional: Add a fallback solid color for browsers that don't support the technique */
  background-color: #8b5cf6;  /* Choose a color that blends with your gradient */
}

.comfortaa-6 {
  font-family: "Comfortaa", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: 50px;
  /* Define the gradient as a background image */
  background-image: linear-gradient(to right, #000000, #185a09ff, #36f91cff, #000000);
  
  /* Clip the background to the text shape */
  -webkit-background-clip: text; /* For WebKit browsers (Chrome, Safari, Edge) */
  background-clip: text;

  /* Make the text transparent so the gradient background shows through */
  -webkit-text-fill-color: transparent; /* For WebKit browsers */
  color: transparent; /* Fallback for other browsers (though less effective without background-clip support) */

  /* Optional: Add a fallback solid color for browsers that don't support the technique */
  background-color: #8b5cf6;  /* Choose a color that blends with your gradient */
}
 </style>
</head>
<body>
    <font class="comfortaa-1">
    Babybib
    </font> <br>
    <font class="comfortaa-2">
    Babybib
    </font>
     <br>
    <font class="comfortaa-3">
    Babybib
    </font>
    <br>
    <font class="comfortaa-4">
    Babybib
    </font>
       <br>
    <font class="comfortaa-5">
    Babybib
    </font>
    <br>
    <font class="comfortaa-6">
    Babybib
    </font>
</body>
</html>