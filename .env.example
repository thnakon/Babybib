# =====================================================
# Babybib Environment Configuration
# =====================================================
# คัดลอกไฟล์นี้เป็น .env และกรอกค่าให้ถูกต้อง
# Copy this file to .env and fill in the correct values
# =====================================================

# Database Configuration
DB_HOST=localhost
DB_NAME=babybib_db
DB_USER=root
DB_PASS=your_secure_password_here
DB_CHARSET=utf8mb4

# Site Configuration
SITE_URL=http://localhost/babybib_db
SITE_NAME=Babybib
SITE_ENV=development
# Options: development, production

# Session Security
SESSION_COOKIE_SECURE=0
# Set to 1 when using HTTPS in production

# User Limits
MAX_BIBLIOGRAPHIES=300
MAX_PROJECTS=30

# Session Timeout (seconds)
SESSION_TIMEOUT=600

# Email Configuration (Optional - for verification emails)
MAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM_NAME=Babybib
SMTP_FROM_EMAIL=noreply@babybib.com

# Timezone
TIMEZONE=Asia/Bangkok

# Debug Mode
DEBUG_MODE=false
# Set to true only in development

# =====================================================
# IMPORTANT: Security Notes
# =====================================================
# 1. Never commit .env to git (it's in .gitignore)
# 2. Use strong passwords in production
# 3. Set DEBUG_MODE=false in production
# 4. Set SESSION_COOKIE_SECURE=1 when using HTTPS
# =====================================================
